# 🚀 ALTRON Security Suite - Инструкция по установке (Deployment Guide)

Эта инструкция предназначена для системных администраторов и DevOps-специалистов.
Приложение написано на **Python (Flask)** и готово к работе в высоконагруженной среде.
app.run(port=4568, debug=False, host='0.0.0.0')(послед строчка app.py)

---

## 1. Системные требования
*   **OS:** Linux (Ubuntu/Debian - рекомендуемая) или Windows Server.
*   **Python:** Версия 3.8 или выше.
*   **Сетевой доступ:** Открытый порт **4568** (или любой другой, если используете Nginx).

---

## 2. Установка системных инструментов (Важно!)
Для работы мощного сканера поддоменов (Subdomain Finder) приложение использует утилиту **Gobuster**.
Приложение умеет работать и без неё (встроенный Python-режим), но с ней сканирование будет быстрее и качественнее.

**Для Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install gobuster
```
*Приложение автоматически обнаружит `gobuster` в системе. Никаких конфигов править не нужно.*

---

## 3. Установка приложения

1.  **Разместите файлы** приложения на сервере (например, в `/opt/altron`).
2.  **Перейдите в папку:**
    ```bash
    cd /opt/altron
    ```
3.  **Установите зависимости Python:**
    ```bash
    pip3 install -r requirements.txt
    ```

---

## 4. Запуск в Production (Много пользователей)

⚠️ **ВНИМАНИЕ:** Не используйте команду `python app.py` для продакшена! Она только для разработки.
Для работы с множеством пользователей используйте WSGI-сервер **Gunicorn**.

### Шаг 1: Установка Gunicorn
```bash
pip3 install gunicorn
```

### Шаг 2: Запуск сервера
Запустите приложение в 4 потока (workers), чтобы оно могло обрабатывать много пользователей одновременно:

```bash
gunicorn -w 4 -b 0.0.0.0:4568 app:app
```

*   `-w 4`: Количество рабочих процессов (рекомендуется: кол-во ядер CPU * 2 + 1).
*   `-b 0.0.0.0:4568`: Слушать на всех интерфейсах на порту 4568.

### Шаг 3: Настройка Nginx и Домена
**Важно:** Для публичного доступа необходимо купить доменное имя **altronwebscanner.ru** (или .com).
Это позволит пользователям находить сервис в поисковиках (Яндекс/Google) и заходить по красивому адресу.


## 4.1 Настройка Firewall (Безопасность)
Для пользователей должны быть доступны только порты **80** (HTTP) и **443** (HTTPS).
Внутренний порт приложения (4568) должен быть закрыт от внешнего мира.

**Команды для настройки UFW (Ubuntu Firewall):**

```bash
# 1. Разрешить SSH (чтобы вы сами не потеряли доступ к серверу!)
sudo ufw allow ssh

# 2. Разрешить веб-трафик (для пользователей)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 3. Включить защиту
sudo ufw enable
```

**Результат:**
*   ✅ Пользователи заходят на сайт через Nginx (порт 80).
*   ✅ Nginx внутри сервера передает запрос приложению (порт 4568).
*   🔒 Пользователи не могут подключиться к порту 4568 напрямую из интернета.
*   🔒 Все остальные порты (базы данных, служебные сервисы) закрыты.

---

## 5. Проверка работоспособности
1.  Откройте в браузере: `http://ВАШ_IP:4568`
2.  Запустите любой скан (например, Port Scanner).
3.  Приложение должно работать быстро, интерфейс не должен блокироваться.

## 6. Технические особенности (Для справки)
*   **Безопасность:** Встроена защита от SSRF (блокировка localhost/192.168.x.x) и Rate Limiting.
*   **Очистка:** Приложение само очищает старые результаты (старше 1 часа) каждые 10 минут во избежание утечек памяти.
*   **Асинхронность:** Все сканирования запускаются в фоновых потоках, основной интерфейс не блокируется.
